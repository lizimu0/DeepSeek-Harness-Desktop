# 安全与维护边界

## 本地服务并不等于免认证

`/balance-card/*` 使用官方 DSH Connection 的 Host/Origin/Cookie 校验。浏览器通过官方启动 URL 换取会话 Cookie；启动器也使用相同流程。不要通过关闭认证或放开跨站请求解决打不开的问题。

根路径返回 401 表示需要认证，404 表示路由未就绪/不存在，500 表示服务错误。任何一种都不是“界面已经能用”。不得因收到 404/500 立即启动第二个同端口服务。

## 凭据与日志

- 不把 `.credentials.yaml`、`.env`、生产会话、认证 Cookie 或启动 token 提交到仓库。
- 启动器新增日志会过滤常见 token、API key、Authorization、Cookie。历史日志可能仍包含旧版本写入的敏感值；分享前手动脱敏。
- 启动器认证状态采用 Windows 当前用户保护并绑定进程身份；不要在工单中上传状态文件。
- 余额 API 禁止自动跨域重定向携带凭据，不根据近似域名猜供应商。

## 安装和进程归属

- 不注册开机自启，不修改系统 Node 或全局运行时。
- 不使用 `taskkill /IM node.exe`。必须区分本工具拥有的进程与用户已有的服务。
- 安装器只管理本仓库维护的插件目标；未知目录/链接、外部依赖及修改过的部署文件会被拒绝。
- 不提供忽略所有权检查的强制覆盖开关。旧版无清单部署先单独备份确认，再迁移。

## 供应商兼容

使用供应商允许的客户端和 API 接入，不把规避服务准入检测作为兼容方案。仓库不包含客户端身份伪装模块。

## 报告问题

请提供：DSH/Node/WebView2 版本、复现步骤、期望与实际结果、已脱敏的错误类别。不要提供 API key、token URL、Cookie 或完整对话。

单元测试、隔离集成和 Windows 编译验证不等于完整安全审计，也不保证任意第三方插件或供应商的可用性。
